<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.e_learningapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ELearningApp">

        <activity android:name=".frm_NavAbout" />
        <activity android:name=".frm_NavMyAccount" />
        <activity android:name=".frm_NavExploreCourse" />
        <activity android:name=".frm_Home"/>
        <activity android:name=".frm_Login" />
        <activity android:name=".frm_Registration" />
        <activity android:name=".frm_OnBoarding3" />
        <activity android:name=".frm_OnBoarding2" />
        <activity android:name=".frm_OnBoarding1" />
        <activity android:name=".frm_SplashScreen">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>